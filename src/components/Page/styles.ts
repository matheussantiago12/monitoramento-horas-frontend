import styled from 'styled-components'

const Container = styled.div`
    height: 100%;
    width: 100%;
    display: flex;
`

const StyledTopbar = styled.div`
    margin-bottom: 45px;
`

const StyledSidebar = styled.div`
    width: 130px;
    height: 100%;
    background-color: black;
`

export { Container, StyledTopbar, StyledSidebar }
